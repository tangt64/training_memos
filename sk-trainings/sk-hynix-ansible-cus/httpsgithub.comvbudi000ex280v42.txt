https://github.com/vbudi000/ex280v42
https://github.com/sandervanvugt/ex280

apiVersion: config.openshift.io/v1  # oc(*), YAML ---> dashboard
kind: OAuth
metadata:
  name: cluster
spec: 
  identityProviders:
  - htpasswd:
      fileData:              
        name: <CHANGE>
    mappingMethod: claim
    name: <CHANGE>
    type: HTPasswd

new-project <PROJECT 1> ## oc, dashboard
            <PROJECT 2>
            <PROJECT 3>

adm policy   ## oc(*), cluster admin
group        ## oc(*)
policy       ## basic-user, admin(project), view, oc(*)

new-app      <APP1>   # oc(*)
             <APP2>   # image, new-app docker://registry.example.com/sds/sdsd
             <APP3>   # github, new-app https://git.example.com/test/test-app

set scale     # oc(*)
hpa scale     # oc(*), YAML

resource limit/quota(pod, container)

expose --hostname <APPLICATION_DOMAIN> 

create/apply -f <YAML>

get pv   # YAML
get pvc  # YAML




